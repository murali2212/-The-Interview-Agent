# Core
fastapi
uvicorn[standard]
pydantic
python-dotenv
python-multipart
pypdf

# LangChain + provider — langchain-core and langchain-groq only; the bare
# `langchain` and `langchain-community` packages are never imported anywhere
# in app/ and just add deploy weight (langchain-community especially pulls
# in a long transitive chain).
langchain-core
langchain-groq

# Tests
pytest
httpx

# Optional — semantic retrieval. The agentic-RAG path (app/knowledge.py) uses
# lexical search over 31 short documents and needs neither of these. Install
# only if you switch to embedding-based retrieval.
# faiss-cpu
# sentence-transformers
# langchain-huggingface
